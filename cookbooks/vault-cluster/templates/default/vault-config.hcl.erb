storage "consul" {
  address = "127.0.0.1:8500"
  path    = "vault2"
}

listener "tcp" {
  address     = "0.0.0.0:8200"
  tls_disable = 1
}

seal "awskms" {
  region     = "#{node[:region]}"
  kms_key_id = "#{node[:vault][:kms_key_id]}"
}
